[package]
name = "delta-desktop"
version = "0.3.1"
description = "Delta desktop shell"
edition = "2021"
rust-version = "1.85"

[lib]
name = "delta_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-dialog = "2"
tauri-plugin-autostart = "2"
tauri-plugin-single-instance = "2"
tauri-plugin-updater = "2"
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4"] }
encoding_rs = "0.8"
# Localhost auth proxy (proxy.rs): async TCP relay for REST + WebSocket upgrade.
tokio = { version = "1", features = ["net", "io-util", "rt"] }
# Kept outside the Tauri shell so another product can depend on the same local STT engine.
delta-stt = { path = "../../../services/stt" }

[dev-dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
