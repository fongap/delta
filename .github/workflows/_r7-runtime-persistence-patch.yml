name: R7 Runtime Persistence Patch

on:
  push:
    branches:
      - codex/r7-runtime-persistence-failclosed

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: codex/r7-runtime-persistence-failclosed
      - name: Apply fail-closed event delivery patch
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          runtime_path = Path('core/runtime-native/src/runtime.rs')
          desktop_path = Path('apps/desktop/src-tauri/src/runtime_ipc.rs')
          runtime = runtime_path.read_text()
          desktop = desktop_path.read_text()

          def replace_once(text: str, old: str, new: str, label: str) -> str:
              if old not in text:
                  raise SystemExit(f'missing replacement target: {label}')
              return text.replace(old, new, 1)

          runtime = replace_once(runtime, '''pub trait EventSink: Send + Sync {
              fn emit(&self, frame: Value);
          }

          /// Default sink: write the frame as one JSON line to stdout.
          pub struct StdoutSink;
          impl EventSink for StdoutSink {
              fn emit(&self, frame: Value) {
                  let mut stdout = std::io::stdout();
                  let _ = writeln!(stdout, "{frame}");
                  let _ = stdout.flush();
              }
          }

          /// Silent sink: drop every event (tests, headless runners).
          pub struct NullSink;
          impl EventSink for NullSink {
              fn emit(&self, _: Value) {}
          }
          ''', '''pub trait EventSink: Send + Sync {
              fn emit(&self, frame: Value) -> Result<(), String>;
          }

          /// Default sink: write the frame as one JSON line to stdout.
          pub struct StdoutSink;
          impl EventSink for StdoutSink {
              fn emit(&self, frame: Value) -> Result<(), String> {
                  let mut stdout = std::io::stdout();
                  writeln!(stdout, "{frame}").map_err(|error| error.to_string())?;
                  stdout.flush().map_err(|error| error.to_string())?;
                  Ok(())
              }
          }

          /// Silent sink: drop every event (tests, headless runners).
          pub struct NullSink;
          impl EventSink for NullSink {
              fn emit(&self, _: Value) -> Result<(), String> {
                  Ok(())
              }
          }
          ''', 'EventSink contract')

          runtime = replace_once(runtime, '''#[derive(Clone)]
          struct RuntimeEventEmitter {
              session_id: String,
              sequence: Arc<Mutex<u64>>,
              sink: Arc<dyn EventSink>,
          }

          impl RuntimeEventEmitter {
              fn emit(&self, event: RuntimeEvent) {
                  let mut sequence = self.sequence.lock().unwrap();
                  *sequence += 1;
                  self.sink.emit(event.to_frame(&self.session_id, *sequence));
              }
          }
          ''', '''#[derive(Clone)]
          struct RuntimeEventEmitter {
              session_id: String,
              sequence: Arc<Mutex<u64>>,
              sink: Arc<dyn EventSink>,
              event_error: Arc<Mutex<Option<String>>>,
          }

          impl RuntimeEventEmitter {
              fn emit(&self, event: RuntimeEvent) {
                  if self.event_error.lock().unwrap().is_some() {
                      return;
                  }
                  let frame = {
                      let mut sequence = self.sequence.lock().unwrap();
                      *sequence += 1;
                      event.to_frame(&self.session_id, *sequence)
                  };
                  if let Err(error) = self.sink.emit(frame) {
                      let mut event_error = self.event_error.lock().unwrap();
                      if event_error.is_none() {
                          *event_error = Some(error);
                      }
                  }
              }
          }
          ''', 'RuntimeEventEmitter')

          runtime = replace_once(runtime, '''    run_id: Option<String>,
              sink: Arc<dyn EventSink>,
          }
          ''', '''    run_id: Option<String>,
              sink: Arc<dyn EventSink>,
              event_error: Arc<Mutex<Option<String>>>,
          }
          ''', 'RuntimeHost event_error field')

          runtime = replace_once(runtime, '''            run_id: None,
                  sink: Arc::new(NullSink),
              }
          ''', '''            run_id: None,
                  sink: Arc::new(NullSink),
                  event_error: Arc::new(Mutex::new(None)),
              }
          ''', 'RuntimeHost event_error init')

          runtime = replace_once(runtime, '''    fn emit_event(&self, event: RuntimeEvent) {
              self.event_emitter().emit(event);
          }

              fn event_emitter(&self) -> RuntimeEventEmitter {
              RuntimeEventEmitter {
                  session_id: self.session_id.clone(),
                  sequence: self.sequence.clone(),
                  sink: self.sink.clone(),
              }
          }
          ''', '''    fn emit_event(&self, event: RuntimeEvent) {
              self.event_emitter().emit(event);
          }

              fn clear_event_error(&self) {
              *self.event_error.lock().unwrap() = None;
          }

              fn ensure_event_delivery(&self) -> Result<(), String> {
              self.event_error
                  .lock()
                  .unwrap()
                  .clone()
                  .map(|error| Err(format!("runtime event delivery failed: {error}")))
                  .unwrap_or(Ok(()))
          }

              fn event_emitter(&self) -> RuntimeEventEmitter {
              RuntimeEventEmitter {
                  session_id: self.session_id.clone(),
                  sequence: self.sequence.clone(),
                  sink: self.sink.clone(),
                  event_error: self.event_error.clone(),
              }
          }
          ''', 'RuntimeHost event helpers')

          runtime = replace_once(runtime, '''        self.emit_event(RuntimeEvent::PermissionRequired {
              tool_call_id: call.id.clone(),
              name: call.name.clone(),
              arguments: call.arguments.clone(),
              reason: reason.to_string(),
          });
              self.ledger_append(
          ''', '''        self.emit_event(RuntimeEvent::PermissionRequired {
              tool_call_id: call.id.clone(),
              name: call.name.clone(),
              arguments: call.arguments.clone(),
              reason: reason.to_string(),
          });
              self.ensure_event_delivery()?;
              self.ledger_append(
          ''', 'approval event delivery')

          runtime = replace_once(runtime, '''        match kind {
              "directory" => self.emit_event(RuntimeEvent::DirectoryRequested {
                  tool_call_id: call.id.clone(),
                  reason: call
                      .arguments
                      .get("reason")
                      .and_then(Value::as_str)
                      .unwrap_or_default()
                      .to_string(),
                  path: call
                      .arguments
                      .get("path")
                      .and_then(Value::as_str)
                      .unwrap_or_default()
                      .to_string(),
                  writable: call
                      .arguments
                      .get("writable")
                      .and_then(Value::as_bool)
                      .unwrap_or(false),
              }),
              "question" => self.emit_event(RuntimeEvent::QuestionRequested {
                  tool_call_id: call.id.clone(),
                  arguments: call.arguments.clone(),
              }),
              "plan" => self.emit_event(RuntimeEvent::PlanProposed {
                  tool_call_id: call.id.clone(),
                  plan: call
                      .arguments
                      .get("plan")
                      .and_then(Value::as_str)
                      .unwrap_or_default()
                      .to_string(),
              }),
              _ => unreachable!(),
          }
              let inbox_id = if self.config.unattended {
          ''', '''        match kind {
              "directory" => self.emit_event(RuntimeEvent::DirectoryRequested {
                  tool_call_id: call.id.clone(),
                  reason: call
                      .arguments
                      .get("reason")
                      .and_then(Value::as_str)
                      .unwrap_or_default()
                      .to_string(),
                  path: call
                      .arguments
                      .get("path")
                      .and_then(Value::as_str)
                      .unwrap_or_default()
                      .to_string(),
                  writable: call
                      .arguments
                      .get("writable")
                      .and_then(Value::as_bool)
                      .unwrap_or(false),
              }),
              "question" => self.emit_event(RuntimeEvent::QuestionRequested {
                  tool_call_id: call.id.clone(),
                  arguments: call.arguments.clone(),
              }),
              "plan" => self.emit_event(RuntimeEvent::PlanProposed {
                  tool_call_id: call.id.clone(),
                  plan: call
                      .arguments
                      .get("plan")
                      .and_then(Value::as_str)
                      .unwrap_or_default()
                      .to_string(),
              }),
              _ => unreachable!(),
          }
              if let Err(error) = self.ensure_event_delivery() {
              self.interactions.cancel(&call.id);
              return Some(ToolResult::failure(&call.id, error));
          }
              let inbox_id = if self.config.unattended {
          ''', 'interaction event delivery')

          runtime = replace_once(runtime, '''        self.emit_event(RuntimeEvent::ToolProposed {
              tool_call_id: call.id.clone(),
              name: call.name.clone(),
              arguments: call.arguments.clone(),
              risk_level: Some(format!("{level:?}")),
          });
              if let Err(error) = self.ledger_append(
          ''', '''        self.emit_event(RuntimeEvent::ToolProposed {
              tool_call_id: call.id.clone(),
              name: call.name.clone(),
              arguments: call.arguments.clone(),
              risk_level: Some(format!("{level:?}")),
          });
              if let Err(error) = self.ensure_event_delivery() {
              return ToolResult::failure(&call.id, error);
          }
              if let Err(error) = self.ledger_append(
          ''', 'tool proposed delivery')

          runtime = replace_once(runtime, '''        self.emit_event(RuntimeEvent::ToolStarted {
              tool_call_id: call.id.clone(),
              name: call.name.clone(),
          });
              if let Err(error) = self.ledger_append(
          ''', '''        self.emit_event(RuntimeEvent::ToolStarted {
              tool_call_id: call.id.clone(),
              name: call.name.clone(),
          });
              if let Err(error) = self.ensure_event_delivery() {
              return ToolResult::failure(&call.id, error);
          }
              if let Err(error) = self.ledger_append(
          ''', 'tool started delivery')

          runtime = replace_once(runtime, '''    fn loop_turn(&mut self) -> Result<String, String> {
              let mut iterations = 0usize;
              let mut turn_retries = 0u32;
              loop {
                  if iterations >= self.config.max_iterations {
          ''', '''    fn loop_turn(&mut self) -> Result<String, String> {
              let mut iterations = 0usize;
              let mut turn_retries = 0u32;
              loop {
                  self.ensure_event_delivery()?;
                  if iterations >= self.config.max_iterations {
          ''', 'loop event guard')

          runtime = replace_once(runtime, '''                self.emit_event(RuntimeEvent::TurnEnd {
                      status: "max_iterations_exceeded".to_string(),
                      iterations,
                  });
                      return Ok("max_iterations_exceeded".to_string());
          ''', '''                self.emit_event(RuntimeEvent::TurnEnd {
                      status: "max_iterations_exceeded".to_string(),
                      iterations,
                  });
                      self.ensure_event_delivery()?;
                  return Ok("max_iterations_exceeded".to_string());
          ''', 'max iterations terminal delivery')

          runtime = replace_once(runtime, '''                self.emit_event(RuntimeEvent::Interrupted { iterations });
                      return Ok("interrupted".to_string());
          ''', '''                self.emit_event(RuntimeEvent::Interrupted { iterations });
                      self.ensure_event_delivery()?;
                  return Ok("interrupted".to_string());
          ''', 'early interrupted delivery')

          runtime = replace_once(runtime, '''                    if self.cancel.load(Ordering::Relaxed) {
                          self.messages
                              .push(json!({"role": "notice", "kind": "interrupted", "ts": now_ts()}));
                          self.emit_event(RuntimeEvent::Interrupted { iterations });
                          return Ok("interrupted".to_string());
                      }
                          if self.drain_steering() {
          ''', '''                    self.ensure_event_delivery()?;
                      if self.cancel.load(Ordering::Relaxed) {
                          self.messages
                              .push(json!({"role": "notice", "kind": "interrupted", "ts": now_ts()}));
                          self.emit_event(RuntimeEvent::Interrupted { iterations });
                          self.ensure_event_delivery()?;
                          return Ok("interrupted".to_string());
                      }
                          if self.drain_steering() {
          ''', 'stream interruption delivery')

          runtime = replace_once(runtime, '''            if self.cancel.load(Ordering::Relaxed) && turn.is_none() {
          ''', '''            self.ensure_event_delivery()?;
              if self.cancel.load(Ordering::Relaxed) && turn.is_none() {
          ''', 'post stream delivery')

          runtime = replace_once(runtime, '''                self.emit_event(RuntimeEvent::Interrupted { iterations });
                      return Ok("interrupted".to_string());
              }
                  let turn = turn.unwrap_or_default();
          ''', '''                self.emit_event(RuntimeEvent::Interrupted { iterations });
                      self.ensure_event_delivery()?;
                  return Ok("interrupted".to_string());
              }
                  let turn = turn.unwrap_or_default();
          ''', 'post stream cancelled delivery')

          runtime = replace_once(runtime, '''            self.emit_event(RuntimeEvent::AssistantMessage {
                  message: assistant_message,
                  text: turn.text.clone(),
                  tool_calls: tool_call_names,
                  reasoning: turn.reasoning.clone(),
                  usage: turn.usage.clone(),
              });
                  if turn.tool_calls.is_empty() {
          ''', '''            self.emit_event(RuntimeEvent::AssistantMessage {
                  message: assistant_message,
                  text: turn.text.clone(),
                  tool_calls: tool_call_names,
                  reasoning: turn.reasoning.clone(),
                  usage: turn.usage.clone(),
              });
                  self.ensure_event_delivery()?;
              if turn.tool_calls.is_empty() {
          ''', 'assistant message delivery')

          runtime = replace_once(runtime, '''                self.emit_event(RuntimeEvent::TurnEnd {
                      status: "completed".to_string(),
                      iterations,
                  });
                      return Ok("completed".to_string());
          ''', '''                self.emit_event(RuntimeEvent::TurnEnd {
                      status: "completed".to_string(),
                      iterations,
                  });
                      self.ensure_event_delivery()?;
                  return Ok("completed".to_string());
          ''', 'completed terminal delivery')

          runtime = replace_once(runtime, '''                self.emit_event(RuntimeEvent::ToolFinished {
                      tool_call_id: tc.id.clone(),
                      name: tc.name.clone(),
                      result: result.output.clone(),
                      error: result.error.clone(),
                  });
                  }
                      self.emit_event(RuntimeEvent::IterationEnd {
          ''', '''                self.emit_event(RuntimeEvent::ToolFinished {
                      tool_call_id: tc.id.clone(),
                      name: tc.name.clone(),
                      result: result.output.clone(),
                      error: result.error.clone(),
                  });
                      self.ensure_event_delivery()?;
              }
                      self.emit_event(RuntimeEvent::IterationEnd {
          ''', 'tool finished delivery')

          runtime = replace_once(runtime, '''            self.emit_event(RuntimeEvent::IterationEnd {
                  iteration: iterations,
              });
                  if self.cancel.load(Ordering::Relaxed) {
          ''', '''            self.emit_event(RuntimeEvent::IterationEnd {
                  iteration: iterations,
              });
                  self.ensure_event_delivery()?;
              if self.cancel.load(Ordering::Relaxed) {
          ''', 'iteration end delivery')

          runtime = replace_once(runtime, '''                self.emit_event(RuntimeEvent::Interrupted { iterations });
                      return Ok("interrupted".to_string());
              }
                  self.drain_steering();
          ''', '''                self.emit_event(RuntimeEvent::Interrupted { iterations });
                      self.ensure_event_delivery()?;
                  return Ok("interrupted".to_string());
              }
                  self.drain_steering();
          ''', 'late interrupted delivery')

          runtime = replace_once(runtime, '''    ) -> Result<Value, String> {
              self.emit_event(RuntimeEvent::TurnStart {
                  input: Value::String(user_input.to_string()),
          ''', '''    ) -> Result<Value, String> {
              self.clear_event_error();
              self.emit_event(RuntimeEvent::TurnStart {
                  input: Value::String(user_input.to_string()),
          ''', 'run clears event error')

          runtime = replace_once(runtime, '''            run_id: self.run_id.clone(),
              });
                  let mut message = json!({"role": "user", "content": user_input, "ts": now_ts()});
          ''', '''            run_id: self.run_id.clone(),
              });
                  self.ensure_event_delivery()?;
              let mut message = json!({"role": "user", "content": user_input, "ts": now_ts()});
          ''', 'run start delivery')

          runtime = replace_once(runtime, '''        let result = self.loop_turn();
              if result.is_err() {
          ''', '''        let result = self.loop_turn().and_then(|status| {
              self.ensure_event_delivery()?;
              Ok(status)
          });
              if result.is_err() {
          ''', 'run terminal event guard')

          runtime = replace_once(runtime, '''    pub fn resume(&mut self) -> Result<Value, String> {
              self.emit_event(RuntimeEvent::TurnStart {
          ''', '''    pub fn resume(&mut self) -> Result<Value, String> {
              self.clear_event_error();
              self.emit_event(RuntimeEvent::TurnStart {
          ''', 'resume clears event error')

          runtime = replace_once(runtime, '''            run_id: self.run_id.clone(),
              });
                  self.ledger_transition("run.started", "system", json!({"kind": "resume"}))?;
          ''', '''            run_id: self.run_id.clone(),
              });
                  self.ensure_event_delivery()?;
              self.ledger_transition("run.started", "system", json!({"kind": "resume"}))?;
          ''', 'resume start delivery')

          runtime = replace_once(runtime, '''    pub fn retry(&mut self) -> Result<Value, String> {
              let tail_is_error = self.messages.iter().rev().any(|m| {
          ''', '''    pub fn retry(&mut self) -> Result<Value, String> {
              self.clear_event_error();
              let tail_is_error = self.messages.iter().rev().any(|m| {
          ''', 'retry clears event error')

          runtime = replace_once(runtime, '''        self.emit_event(RuntimeEvent::TurnStart {
              input: Value::String(String::new()),
              attachments: Vec::new(),
              source: None,
              run_id: self.run_id.clone(),
          });
              self.ledger_transition("run.started", "system", json!({"kind": "retry"}))?;
          ''', '''        self.emit_event(RuntimeEvent::TurnStart {
              input: Value::String(String::new()),
              attachments: Vec::new(),
              source: None,
              run_id: self.run_id.clone(),
          });
              self.ensure_event_delivery()?;
          self.ledger_transition("run.started", "system", json!({"kind": "retry"}))?;
          ''', 'retry start delivery')

          runtime = replace_once(runtime, '''            RuntimeCommand::SwitchModel { change, reply } => {
                  let notice = match change {
          ''', '''            RuntimeCommand::SwitchModel { change, reply } => {
                  host.clear_event_error();
                  let notice = match change {
          ''', 'model switch clears event error')

          runtime = replace_once(runtime, '''                sync_message_snapshot(&host, &messages);
                      let _ = reply.send(Ok(notice));
          ''', '''                sync_message_snapshot(&host, &messages);
                      let result = host.ensure_event_delivery().map(|_| notice);
                  let _ = reply.send(result);
          ''', 'model switch propagates event error')

          runtime = replace_once(runtime, '''    impl EventSink for CaptureSink {
              fn emit(&self, frame: Value) {
                  self.frames.lock().unwrap().push(frame);
              }
          }
          ''', '''    impl EventSink for CaptureSink {
              fn emit(&self, frame: Value) -> Result<(), String> {
                  self.frames.lock().unwrap().push(frame);
                  Ok(())
              }
          }
          ''', 'CaptureSink result')

          runtime = replace_once(runtime, '''        let emitter = RuntimeEventEmitter {
              session_id: "session-1".to_string(),
              sequence: Arc::new(Mutex::new(0)),
              sink: sink.clone(),
          };
          ''', '''        let emitter = RuntimeEventEmitter {
              session_id: "session-1".to_string(),
              sequence: Arc::new(Mutex::new(0)),
              sink: sink.clone(),
              event_error: Arc::new(Mutex::new(None)),
          };
          ''', 'provider event emitter test')

          runtime = replace_once(runtime, '''    #[derive(Default)]
              struct CaptureSink {
              frames: Mutex<Vec<Value>>,
          }

              struct MockProvider {
          ''', '''    #[derive(Default)]
              struct CaptureSink {
              frames: Mutex<Vec<Value>>,
          }

              struct FailOnTurnEndSink {
              frames: Mutex<Vec<Value>>,
          }

              impl EventSink for FailOnTurnEndSink {
              fn emit(&self, frame: Value) -> Result<(), String> {
                  if frame.get("type").and_then(Value::as_str) == Some("turn_end") {
                      return Err("simulated persistence failure".to_string());
                  }
                  self.frames.lock().unwrap().push(frame);
                  Ok(())
              }
          }

              struct MockProvider {
          ''', 'failing sink fixture')

          runtime = replace_once(runtime, '''    #[test]
              fn runtime_handle_runs_in_background_and_rejects_parallel_run() {
          ''', '''    #[test]
              fn event_sink_failure_fails_run_and_suppresses_success_terminal_event() {
              let sink = Arc::new(FailOnTurnEndSink {
                  frames: Mutex::new(Vec::new()),
              });
              let mut host = RuntimeHost::new(
                  "session-persistence-failure",
                  RuntimeConfig {
                      max_iterations: 0,
                      ..RuntimeConfig::default()
                  },
              )
              .with_event_sink(sink.clone());

              let result = host.run("hello", None);
              assert!(result
                  .as_ref()
                  .err()
                  .is_some_and(|error| error.contains("runtime event delivery failed")));
              let frames = sink.frames.lock().unwrap();
              let event_types = frames
                  .iter()
                  .filter_map(|frame| frame.get("type").and_then(Value::as_str))
                  .collect::<Vec<_>>();
              assert_eq!(event_types, vec!["turn_start"]);
          }

              #[test]
              fn runtime_handle_runs_in_background_and_rejects_parallel_run() {
          ''', 'event failure regression test')

          desktop = replace_once(desktop, '''impl EventSink for TauriEventSink {
              fn emit(&self, frame: Value) {
                  let _ = delta_runtime_native::control_plane::record_runtime_event(&self.state_dir, &frame);
              if frame.get("type").and_then(Value::as_str) == Some("turn_end") {
                  if let (Some(session_id), Some(status)) = (
                      frame.get("sessionId").and_then(Value::as_str),
                      frame
                          .get("payload")
                          .and_then(|payload| payload.get("status"))
                          .and_then(Value::as_str),
                  ) {
                      if session_id.starts_with("__run__") {
                          let _ = self
                              .automations
                              .lock()
                              .unwrap()
                              .finalize_session(session_id, status);
                      }
                  }
              }
                  let _ = self.app.emit("delta-runtime-event", frame);
              }
          }
          ''', '''impl EventSink for TauriEventSink {
              fn emit(&self, frame: Value) -> Result<(), String> {
                  delta_runtime_native::control_plane::record_runtime_event(&self.state_dir, &frame)
                  .map_err(|error| format!("persist runtime event: {error}"))?;
              if frame.get("type").and_then(Value::as_str) == Some("turn_end") {
                  if let (Some(session_id), Some(status)) = (
                      frame.get("sessionId").and_then(Value::as_str),
                      frame
                          .get("payload")
                          .and_then(|payload| payload.get("status"))
                          .and_then(Value::as_str),
                  ) {
                      if session_id.starts_with("__run__") {
                          self.automations
                              .lock()
                              .unwrap()
                              .finalize_session(session_id, status)
                              .map_err(|error| format!("finalize automation session: {error}"))?;
                      }
                  }
              }
                  self.app
                  .emit("delta-runtime-event", frame)
                  .map_err(|error| format!("emit runtime event: {error}"))?;
              Ok(())
              }
          }
          ''', 'TauriEventSink propagation')

          runtime_path.write_text(runtime)
          desktop_path.write_text(desktop)
          PY
          rustfmt --edition 2021 core/runtime-native/src/runtime.rs apps/desktop/src-tauri/src/runtime_ipc.rs
          git diff --check
      - name: Commit patch and remove staging workflow
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/_r7-runtime-persistence-patch.yml
          git add core/runtime-native/src/runtime.rs apps/desktop/src-tauri/src/runtime_ipc.rs
          git diff --cached --check
          git commit -m "fix(r7): fail closed on runtime event persistence errors"
          git push origin HEAD:codex/r7-runtime-persistence-failclosed
